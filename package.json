{
  "name": "planx-semantic",
  "private": true,
  "scripts": {
    "build": "cd semantic && gulp build-css build-assets",
    "build:watch": "yarn build && gulp watch",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d semantic/dist"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "gulp": "^3.9.1",
    "semantic-ui": "^2.3.1"
  }
}
